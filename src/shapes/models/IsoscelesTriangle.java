package shapes.models;

public class IsoscelesTriangle extends Triangle{
    public IsoscelesTriangle(String label, String color, float twinEdges, float differentEdge) {
        super(label, color, twinEdges, twinEdges, differentEdge);
    }
}
