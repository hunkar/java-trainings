package shapes.models;

public class EquilateralTriangle extends Triangle{
    public EquilateralTriangle(String label, String color, float edge) {
        super(label, color, edge, edge, edge);
    }
}
